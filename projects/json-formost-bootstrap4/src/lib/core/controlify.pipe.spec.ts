import { ControlifyPipe } from './controlify.pipe';

describe('ControlifyPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlifyPipe();
    expect(pipe).toBeTruthy();
  });
});
