import { FormGroup as NgFormGroup, AbstractControl } from '@angular/forms';
import { FormostAbstractControl } from '../formost-abstract-control.interface';
export declare class FormGroup<T extends object = any> extends NgFormGroup implements FormostAbstractControl {
    controls: {
        [key: string]: AbstractControl;
    };
    readonly value: T;
    constructor(controls: {
        [key: string]: AbstractControl;
    });
    name: string;
    refid: string;
    title?: string;
    description?: string;
    help?: string;
    populate(source: object): void;
    getControlType(): "control" | "group" | "array";
}
