import { ControlKeyValuePipe } from './control-key-value.pipe';

describe('ControlKeyValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ControlKeyValuePipe();
    expect(pipe).toBeTruthy();
  });
});
